    </main>
    
    <footer class="footer mt-auto py-3">
        <div class="container text-center">
            <span class="text-body-secondary">
                <%= constants.APP_NAME %> v<%= constants.APP_VERSION %> |
                <a href="<%= constants.GITHUB_REPO %>" 
                   target="_blank" 
                   class="text-decoration-none text-body-secondary">
                    <i class="bi bi-github"></i> GitHub
                </a>
            </span>
            <div class="mt-2 d-flex gap-2 justify-content-center">
                <a href="/blog" class="text-body-secondary text-decoration-none">
                    <i class="bi bi-newspaper me-1"></i>Blog
                </a>
                <a href="/discord" class="text-body-secondary text-decoration-none">
                    <i class="bi bi-discord me-1"></i>Discord
                </a>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const navLinks = document.querySelectorAll('.nav-link');
        
        document.addEventListener('DOMContentLoaded', () => {
            navLinks.forEach(link => {
                if (link.href === window.location.href) {
                    link.classList.add('active');
                } else {
                    link.classList.remove('active');
                }
            });
        });
    </script>
</body>
</html> 